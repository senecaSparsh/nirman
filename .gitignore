node_modules
.turbo
.next
dist
.env
.env.local
.env.*.local
*.tsbuildinfo
coverage
.DS_Store
.playwright-mcp
Screenshot 2026-07-30 at 7.29.34 PM.png

# Root-level dev screenshots
/*.png

# Uploaded files — stored outside public/ for auth-gated access
storage/

# Source audio + raw transcripts (large, not code)
*.m4a
*.m4a.json
*_raw/

# Python bytecode cache
__pycache__/
*.pyc

# Env backups (secrets)
.env.bak
.env.*.bak

# Runtime call recordings / uploaded audio
apps/web/data/

# Playwright test artifacts (regenerate on every run — never commit)
test-results/
playwright-report/
playwright/.cache/
onboarding-queue.txt

# Sentry build artifacts
.sentry/
