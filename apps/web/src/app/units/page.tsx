import { PagePlaceholder } from "@/components/page-placeholder";

export default function UnitsPage() {
  return (
    <PagePlaceholder
      title="Built Units"
      description="Per-project unit inventory — BHKs, shops, offices, warehouse units — with status, pricing and valuation."
      phase="Phase 2"
    />
  );
}
