import { PagePlaceholder } from "@/components/page-placeholder";

export default function MaterialsPage() {
  return (
    <PagePlaceholder
      title="Material Inventory"
      description="Material catalog, stock by location (company warehouse / project sites), and low-stock alerts."
      phase="Phase 1"
    />
  );
}
