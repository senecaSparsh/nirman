import { PagePlaceholder } from "@/components/page-placeholder";

export default function FinancePage() {
  return (
    <PagePlaceholder
      title="Finance & Reports"
      description="Project P&L, inventory valuation, unsold asset value, cost breakdown, expenses and audit log."
      phase="Phase 4"
    />
  );
}
