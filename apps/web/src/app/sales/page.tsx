import { PagePlaceholder } from "@/components/page-placeholder";

export default function SalesPage() {
  return (
    <PagePlaceholder
      title="Sales"
      description="Asset sales (land or built units), payments, installments, invoices and profit calculation."
      phase="Phase 3"
    />
  );
}
