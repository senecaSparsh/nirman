import { PagePlaceholder } from "@/components/page-placeholder";

export default function StockMovementsPage() {
  return (
    <PagePlaceholder
      title="Stock Movements"
      description="The immutable material ledger — receipts, transfers, issues, adjustments. Issue materials to a project."
      phase="Phase 1"
    />
  );
}
