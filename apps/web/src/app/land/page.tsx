import { PagePlaceholder } from "@/components/page-placeholder";

export default function LandPage() {
  return (
    <PagePlaceholder
      title="Land"
      description="Land purchases, parcels, and the partition action to split a parcel into sellable sub-plots."
      phase="Phase 2"
    />
  );
}
